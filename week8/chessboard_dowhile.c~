#include <stdio.h>
int main()
{int col,row=0;
int n;

printf("n?\n");
scanf("%d",&n);

do
{	col=0;
	do
	{ 	
		if(row%2==0){
		if(col%2==0)
		printf("0 ");
		else
		printf("X ");
		}
	else	
		{
		if(col%2!=0)
		printf("0 ");
		else
		printf("X ");	
		
		} 
	col++;
	}
	while(col<n);
printf("\n");
row++;
}
while(row<n);


	return 0;
}
