#include<stdio.h>
int main()
{	int n,i,a;
 
	printf("enter a number\n");
	scanf("%d",&n);
	
	a=n-2;
	
	for(i=2;i<n;i++)
	{
		if(a%i==0)
		{
		if((i*i)==(int)a)
		{printf("YES\n");
		return 0;}
		}
	}
	
	printf("NO\n");
	return 0;
}
